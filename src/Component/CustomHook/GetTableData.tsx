import React, { useCallback, useEffect, useState } from 'react'

const useGetTableData = ({ctx}: {ctx: string}) => {
  const [data, setData] = useState();
  const getData = useCallback(async () => {
    
  },[])
  useEffect(() => {

  }, [])
  return (
    <div>

    </div>
  )
}

export default useGetTableData